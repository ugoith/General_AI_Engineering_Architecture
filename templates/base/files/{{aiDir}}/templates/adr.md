# ADR NNNN：<决策标题>

状态：proposed
日期：{{date}}
决策者：{{owner}}
影响范围：<模块 / 契约 / 构建 / 流程>
关联：<相关 ADR 编号、任务包或 issue>

## Context（背景与约束）

<为什么现在必须做这个决定？列出推动因素与硬约束：性能、期限、兼容性、团队规模、外部依赖。
只写事实，不写倾向；读者应能据此判断"不做决定会怎样"。>

## Decision（决定）

<第一句用"我们决定……"写清结论，然后写具体做法：>

- 采用什么、边界在哪、何时生效
- 涉及的路径、接口或依赖版本
- 明确**不做**什么（防止范围蔓延）

## Consequences（后果）

- 正面：<获得什么>
- 代价：<失去什么、增加哪些长期维护成本>
- 需要同步更新：<按 `{{aiDir}}/index/impact-map.json` 列出具体文件>

## Alternatives considered（考虑过的替代方案）

| 方案 | 为什么没选 |
|---|---|
| <方案 A> | <理由，最好带事实或数字> |
| <保持现状、什么都不做> | <理由：为什么现状不可接受> |

## 影响面检查清单

- [ ] 数据模型 / schema 变更 → 已更新 `{{aiDir}}/registry.json` 与数据模型文档
- [ ] 接口 / 契约变更 → 已更新接口文档，并确认了全部调用方
- [ ] 模块边界变更 → 已更新架构概览与依赖方向说明
- [ ] 依赖 / 构建变更 → 已更新锁文件、构建脚本与本地开发说明
- [ ] 公开行为变更 → 已补测试与变更记录
- [ ] 索引同步：受影响的文件摘要已重写，`node {{aiDir}}/bin/ai-arch.mjs review --drift` 无新增漂移
