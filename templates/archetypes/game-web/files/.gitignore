# {{projectTitle}} — 由框架 {{frameworkVersion}} 生成（{{date}}）。可按项目需要增删，但保持跨平台。
# 注意：本文件是"基础规则 + Web 游戏专属规则"的完整版；archetype 覆盖 base 同名文件时是整份替换，不做合并。

## 依赖与虚拟环境
node_modules/
.pnpm-store/
.vite/
.parcel-cache/

## 构建产物
dist/
dist-ssr/
build/
out/
*.tsbuildinfo
*.min.js.map

## 测试与覆盖率
coverage/
playwright-report/
test-results/
.nyc_output/

## 日志与临时文件
*.log
logs/
tmp/
temp/
.cache/
*.tmp
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

## 环境变量与密钥（绝不提交）
.env
.env.*
.env.local
.env.*.local
!.env.example
*.pem
*.key
*.p12
secrets.*

## 编辑器与系统
.idea/
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

## AI 上下文：只忽略缓存。索引、任务包、决策记录都是共享资产，必须提交。
{{aiDir}}/cache/

## 不要忽略以下内容：
##   {{aiDir}}/index/files.json  —— 文件索引（hash + 摘要），提交后每个任务才不用重读源码
##   {{aiDir}}/bin/ 与 {{aiDir}}/lib/ —— 项目内的 ai-arch 工具链，供离线使用
##   {{srcDir}}/assets/** —— 资源清单与资源本体要入库（否则无法复现构建）
