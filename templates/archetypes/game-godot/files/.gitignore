# {{projectTitle}} — 由框架 {{frameworkVersion}} 生成（{{date}}）。可按项目需要增删，但保持跨平台。
# 本文件是**完整版**：基础层通用规则 + Godot 引擎专属规则。渲染器按路径整份覆盖，不会与 base 合并，因此通用规则必须保留在这里。

## 依赖与虚拟环境
node_modules/
.pnpm-store/
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
vendor/

## 构建产物
dist/
build/
out/
target/
obj/
*.nupkg

## 测试与覆盖率
coverage/
.nyc_output/
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/

## 日志与临时文件
*.log
logs/
tmp/
temp/
.cache/
*.tmp

## 环境变量与密钥（绝不提交）
.env
.env.*
!.env.example
*.pem
*.key
*.p12
secrets.*

## 编辑器与系统
.idea/
.vscode/
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

## AI 上下文：只忽略缓存。索引、任务包、决策记录都是共享资产，必须提交。
{{aiDir}}/cache/

## ── Godot 引擎专属：生成物与本地缓存（都不入库）────────────────
## 编辑器导入/着色器/缓存目录（重新打开项目会自动重建）
.godot/

## 导出产物（目录名大小写都覆盖）
Build/
export/
*.pck
*.zip

## 平台导出产物
*.exe
*.dll
*.so
*.dylib
*.apk
*.aab
*.ipa
*.wasm
android/build/
ios/

## 不要忽略以下内容：
##   scenes/** 与 assets/** 场景与资源是项目真身；`assets/**` 的 .import 也要入库
##   project.godot 与 export_presets.cfg 是核心配置，必须入库
##   {{aiDir}}/index/files.json 文件索引（hash + 摘要），提交后每个任务才不用重读源码
##   {{aiDir}}/bin/ 与 {{aiDir}}/lib/ 项目内的 ai-arch 工具链，供离线使用
